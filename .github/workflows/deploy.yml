name: Deploy to Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Sync files to server
        run: |
          ssh-keyscan -H 94.159.102.126 >> ~/.ssh/known_hosts
          rsync -avz --delete ./ root@94.159.102.126:/Test