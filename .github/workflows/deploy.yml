name: Build and Deploy MkDocs

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. Клонируем код репозитория
      - name: Checkout code
        uses: actions/checkout@v3

#      # 2. Устанавливаем Python и зависимости
#      - name: Set up Python
#        uses: actions/setup-python@v4
#        with:
#          python-version: "3.10"
#
#      - name: Install dependencies
#        run: |
#          python -m venv venv
#          source venv/bin/activate
#          pip install -r requirements.txt
#          pip install mkdocs

      # 3. Сборка проекта MkDocs
      - name: Build MkDocs site
        run: |
          source venv/bin/activate
          mkdocs build

      # 4. Копирование файлов на сервер
      - name: Deploy to Server
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        run: |
          mkdir -p ~/.ssh
          echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H 94.159.102.126 >> ~/.ssh/known_hosts
          rsync -avz --delete site/ root@94.159.102.126:/var/www/html/